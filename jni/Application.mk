#APP_PROJECT_PATH := $(call my-dir)
#LOCAL_ARM_MODE := arm
APP_ABI := armeabi armeabi-v7a
#APP_MODULES  := SpokenPic
#APP_MODULES      += libvorbis-stream
#APP_MODULES      += libvorbis
#APP_MODULES      += libogg
#APP_MODULES      += speex

APP_OPTIM        := release 
APP_CFLAGS       += -O3 
